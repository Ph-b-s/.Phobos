# Phobos first-run setup

Phobos automatically detects available hardware, chooses the most suitable local Mistral model, starts the local inference runtime, and downloads the model when needed.

Normal users do not need to know the model name or configure an API key.
