[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "phobos-ai-security"
version = "0.1.1"
description = "AI security reconnaissance and attack-surface mapping framework"
requires-python = ">=3.11"
readme = "README.md"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
phobos = "cli:main"

[tool.setuptools]
py-modules = [
    "ai",
    "cli",
    "config",
    "crawler",
    "evidence",
    "graph",
    "models",
    "nodes",
    "nmap_runner",
    "request_manager",
    "scope",
]

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["test_*.py"]
